# Local development: libSQL with a local file (no auth token needed)
TURSO_DATABASE_URL=file:./local.db
TURSO_AUTH_TOKEN=

# Production (set these in Vercel project settings, do NOT commit):
# TURSO_DATABASE_URL=libsql://<your-db>-<org>.turso.io
# TURSO_AUTH_TOKEN=<token from `turso db tokens create <db>`>

# 워크스페이스 슈퍼관리자 — 쉼표로 구분된 세션 ID 목록.
# 카카오 사용자는 "kakao:<숫자ID>" 형태입니다. (예: SUPER_ADMIN_IDS=kakao:1234567890)
# 여기에 등록된 사용자만 슈퍼관리자가 되며, DB로는 변경할 수 없습니다.
# 본인 ID 확인: 한 번 로그인한 뒤 `npm run whoami` 실행.
SUPER_ADMIN_IDS=
